import { Hono } from "hono";

const blogRouter = new Hono();


// blogRouter.post("/",(c)=>{

// })

// blogRouter.put("/", (c)=>{
    
// })

// blogRouter.get("/:id", (c)=>{

// })

// blogRouter.get("/bulk", (c)=>{

// })

export default blogRouter;